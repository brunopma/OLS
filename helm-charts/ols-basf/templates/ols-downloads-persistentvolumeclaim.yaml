apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    helm.sh/resource-policy: "keep"
  labels:
    app: ols-downloads
  name: ols-downloads
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
